# php-cli-repository-tools

Configuration

If vendor directory is missing please run

```
composer dump-autoload
```

Use

On linux:
```
chmod +x app
./app [--service xyz] owner/repo branchName
```

On windows:
```
path/to/php.exe app [--service xyz] owner/repo branchName
```
